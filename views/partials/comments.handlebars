<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{format_date created_at}}, {{user.username}} 
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>